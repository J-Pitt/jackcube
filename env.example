# Upstash Redis (same as truthordare — copy from ../truthordare/.env.local or project root)
UPSTASH_REDIS_REST_URL=https://your-db.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token

# Public site URL for invite links & QR (set in Amplify env vars)
# NEXT_PUBLIC_APP_URL=https://your-amplify-domain.amplifyapp.com
